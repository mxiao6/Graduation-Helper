language: node_js
node_js:
 - "stable"
 
before_script:
  - npm install
  - npm install -g mocha
  - npm install -g selenium-webdriver
  - npm install -g chai
  
addons:
    chrome: stable

script:
  - mocha test/selenium
